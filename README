A simple linker to combine object files generated by a compiler into a single executable program. Should probably have been written in C, but was interested to learn about working with bitwise operations and binary files in Java.
